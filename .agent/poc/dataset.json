[
  {
    "inputs": {
      "input": "Summarize the file mlflow/version.py"
    },
    "expectations": {
      "required_sections": "Purpose, Key Functions, Dependencies"
    }
  },
  {
    "inputs": {
      "input": "Summarize the file mlflow/runs.py"
    },
    "expectations": {
      "required_sections": "Purpose, Key Functions, Dependencies"
    }
  },
  {
    "inputs": {
      "input": "Summarize the file mlflow/environment_variables.py"
    },
    "expectations": {
      "required_sections": "Purpose, Key Functions, Dependencies"
    }
  }
]
